# --- Web ---
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31     # includes httptools, uvloop, websockets

# --- Database / ORM ---
SQLAlchemy>=2.0,<2.1
asyncpg>=0.29,<0.30               # async Postgres driver
alembic>=1.13,<1.14               # migrations (sync)

# --- Auth / Security ---
passlib[bcrypt]>=1.7,<1.8
python-jose>=3.3,<3.4

# --- HTTP client ---
httpx>=0.27,<0.28                 # async HTTP client (for outbound calls)

# --- Optional, add if used ---
python-multipart>=0.0.9         # file uploads via FastAPI forms
email-validator>=2.2            # Pydantic EmailStr fields
python-dotenv>=1.0              # load .env in local dev
